node_modules/
dist/
.next/
out/
build/
.turbo/
.cache/

# Env
.env
.env.local
.env.*.local

# OS / editors
.DS_Store
*.log
.idea/
.vscode/
*.swp

# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/

# TypeScript build info
*.tsbuildinfo

# Coverage
coverage/

# Large binary fallback (download locally — see web/public/README.md)
web/public/demo.vrm

# Preset + uploaded avatars — keep the directories (via README) but
# skip the binaries so the repo stays small.
web/public/presets/*.vrm
web/public/uploads/*.vrm
