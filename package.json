{
  "name": "gleamers",
  "version": "0.1.0",
  "private": true,
  "description": "AI VTuber streaming platform with on-chain tokenomics",
  "scripts": {
    "dev": "concurrently -n web,worker,orch -c cyan,magenta,yellow \"pnpm --filter web dev\" \"pnpm --filter worker dev\" \"pnpm --filter orchestrator dev\"",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.5.4"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  }
}
